# Google Maps key used by all maps (Recherche, sujets, cartes interactives)
# Create in Google Cloud with billing enabled and Maps JavaScript + Places (+ Geocoding if needed).
VITE_GOOGLE_MAPS_API_KEY=YOUR_PRODUCTION_GOOGLE_MAPS_API_KEY
